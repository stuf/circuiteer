/**
 * @typedef {object} ModuleObject
 * @prop {string} id
 * @prop {string} shortId
 * @prop {string} name
 * @prop {number} power
 * @prop {PowerType} powerType
 * @prop {tier} number
 * @prop {number[]} size
 */

/**
 * @typedef {string} PowerType
 */
