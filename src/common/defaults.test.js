import * as Default from './defaults';

test('BaseSize', () => {
  expect(Default['BaseSize']).not.toBeUndefined();
});

test('SizeScale', () => {
  expect(Default['SizeScale']).not.toBeUndefined();
});

test('DefaultSize', () => {
  expect(Default['DefaultSize']).not.toBeUndefined();
});
